1.clone the repo using command git clone "repo link"
2.After clonning the repo open the project into vs code and open vs code terminal 
3. To install all the packges write npm i into the terminal and it will automatically install the required packages
4. After intsalling all the packages into the project run npm test to run the project
5. After running the project open browser and  hit the url localhost:3000 for the 
6. If you are new user then hit th new user button to register a new user

localhost:3000